# # ApacheWatch Configuration

# server_name: "my-server"

# # Apache log paths - adjust for your system
# apache:
#   error_log: "/var/log/apache2/error.log"
#   access_log: "/var/log/apache2/access.log"

# # Where to store collected data
# data_file: "./data/apachewatch.json"

# # Web server settings
# web:
#   host: "0.0.0.0"
#   port: 8080

# configuration for my TEST fake Apache logs
server_name: "local-dev"

apache:
  error_log: ./logs/error.log
  access_log: ./logs/access.log

database: ./data/apachewatch.db

alerts:
  enabled: true
  cooldown_minutes: 30
  thresholds:
    disk_percent: 85
    server_errors_5xx: 5
    client_errors_4xx: 50
    requests_per_ip: 100
    bot_percentage: 75

web:
  host: "0.0.0.0"  # Use 0.0.0.0 for Docker, or 127.0.0.1 for local-only
  port: 8080
